to load the chosen yolo model via ultralytics:

python3 -m venv yolo_export
source yolo_export/bin/activate
pip install --upgrade pip
pip install ultralytics onnx

then

python3 modelsrc.py

edit modelsrc.py if necessary
